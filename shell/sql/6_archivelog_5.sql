set feedback off;
set timing off;
set pagesize 30
SET LINESIZE 1024;
SET COLSIZE 60;
select
       'The current version does not support this view yet(시간대별로 생성된 archivelog 갯수 표시 )!!!' as Message
from dual 
--sqlend
;
